---
name: User role
description: User works in Fishbowl ERP and maintains embedded HTML/JS BI tools that run inside the Fishbowl client
type: user
---

User (kye.tonkin@fbinv.com) develops and maintains single-file HTML/JS "BI report" pages embedded in Fishbowl ERP. They run inside Fishbowl's JxBrowser 8 (Chromium) panel. Cannot run the page outside Fishbowl — no browser dev tools in the target environment, so debugging relies on an in-page debug console and instrumentation baked into the script. Local testing only verifies syntax/logic, not runtime behaviour. Projects live under C:\Users\kye.tonkin\Desktop\FB_BIReports\ (one folder per tool). The house style is a polished single-file dashboard; see [[project_wo_finisher]] and the sibling QC StockRelease tool.
