{
  "_comment": "Shape of a single TENANTS KV entry. The KV KEY is the short tenant code the admin gives the user (e.g. 'exampleco'); this JSON is the VALUE. `server` is the tunnel hostname that fronts that tenant's on-prem Fishbowl REST API. Store one entry per tenant. End users never enumerate this namespace.",
  "name": "Example Co",
  "server": "https://fb-example.example.com"
}
